// eslint-disable-next-line import/prefer-default-export
export enum TransactionStatusEnum {
  COMPLETED = 'completed',
  PENDING = 'pending',
  CANCELLED = 'cancelled',
  FAILED = 'failed',
  REMINDER = 'reminder',
}
